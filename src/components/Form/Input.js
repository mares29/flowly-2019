import styled from "styled-components"
import colors from "../../constants/colors"

export default styled.input`
  position: relative;
  border: none;
  width: 100%;
  background: transparent;
  padding: 20px 0;
  border-bottom: solid 10px ${colors.creative};
  color: rgba(255, 255, 255, 0.85);
  font-style: italic;
  margin-bottom: 40px;
  outline: none;
`
